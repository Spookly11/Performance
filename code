--// Performance UI
--// Part 1A (Lines 1-100)

--==================================================
-- Duplicate Protection
--==================================================

local GUI_NAME = "PerformanceUI"

local Player = game:GetService("Players").LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

local Existing = PlayerGui:FindFirstChild(GUI_NAME)

if Existing then
    Existing:Destroy()
end

--==================================================
-- Services
--==================================================

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

--==================================================
-- Theme
--==================================================

local Theme = {
    Background = Color3.fromRGB(24,24,24),
    BackgroundTransparency = 0.10,

    Accent = Color3.fromRGB(70,70,70),

    Text = Color3.new(1,1,1),
    SecondaryText = Color3.fromRGB(185,185,185),

    Red = Color3.fromRGB(221,28,28),
    Green = Color3.fromRGB(0,255,98)
}

--==================================================
-- GUI
--==================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = GUI_NAME
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

--==================================================
-- Main Window
--==================================================

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.fromOffset(255,165)
Main.Position = UDim2.new(0.03,0,0.35,0)
Main.BackgroundColor3 = Theme.Background
Main.BackgroundTransparency = Theme.BackgroundTransparency
Main.BorderSizePixel = 0
Main.Active = true
Main.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0,12)
Corner.Parent = Main

local Stroke = Instance.new("UIStroke")
Stroke.Color = Theme.Accent
Stroke.Thickness = 1
Stroke.Transparency = 0.35
Stroke.Parent = Main

--==================================================
-- Shadow
--==================================================

local Shadow = Instance.new("ImageLabel")
Shadow.Name = "Shadow"
Shadow.AnchorPoint = Vector2.new(0.5,0.5)
Shadow.Position = UDim2.new(0.5,0,0.5,4)
Shadow.Size = UDim2.new(1,32,1,32)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://1316045217"
Shadow.ImageTransparency = 0.55
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(10,10,118,118)
Shadow.ZIndex = -1
Shadow.Parent = Main

--==================================================
-- Title
--==================================================

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.BackgroundTransparency = 1
Title.Position = UDim2.fromOffset(16,10)
Title.Size = UDim2.fromOffset(180,24)
Title.Font = Enum.Font.GothamBold
Title.Text = "Performance"
Title.TextColor3 = Theme.Text
Title.TextSize = 18
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

--==================================================
-- Divider
--==================================================

local Divider = Instance.new("Frame")
Divider.BorderSizePixel = 0
Divider.BackgroundColor3 = Theme.Accent
Divider.BackgroundTransparency = 0.55
Divider.Position = UDim2.fromOffset(12,40)
Divider.Size = UDim2.new(1,-24,0,1)
Divider.Parent = Main
  --==================================================
-- Gear Button
--==================================================

local GearButton = Instance.new("ImageButton")
GearButton.Name = "GearButton"
GearButton.Size = UDim2.fromOffset(24,24)
GearButton.Position = UDim2.new(1,-36,0,10)
GearButton.BackgroundTransparency = 1
GearButton.AutoButtonColor = false
GearButton.Image = "rbxassetid://3926305904"
GearButton.ImageRectOffset = Vector2.new(964,324)
GearButton.ImageRectSize = Vector2.new(36,36)
GearButton.ImageColor3 = Theme.Text
GearButton.Parent = Main

--==================================================
-- Tooltip
--==================================================

local Tooltip = Instance.new("TextLabel")
Tooltip.Name = "Tooltip"
Tooltip.AnchorPoint = Vector2.new(0.5,1)
Tooltip.Position = UDim2.new(0.5,0,0,-6)
Tooltip.Size = UDim2.fromOffset(72,24)
Tooltip.BackgroundColor3 = Color3.fromRGB(35,35,35)
Tooltip.BackgroundTransparency = 0.15
Tooltip.Text = "Settings"
Tooltip.Font = Enum.Font.Gotham
Tooltip.TextSize = 13
Tooltip.TextColor3 = Theme.Text
Tooltip.TextTransparency = 1
Tooltip.Visible = false
Tooltip.Parent = GearButton

local TooltipCorner = Instance.new("UICorner")
TooltipCorner.CornerRadius = UDim.new(0,6)
TooltipCorner.Parent = Tooltip

--==================================================
-- Tween Helper
--==================================================

local function Tween(Object, Time, Properties, Style, Direction)
	return TweenService:Create(
		Object,
		TweenInfo.new(
			Time,
			Style or Enum.EasingStyle.Quad,
			Direction or Enum.EasingDirection.Out
		),
		Properties
	)
end

--==================================================
-- Tooltip Animation
--==================================================

GearButton.MouseEnter:Connect(function()

	Tooltip.Visible = true

	Tween(Tooltip,0.15,{
		TextTransparency = 0
	}):Play()

	Tween(GearButton,0.15,{
		Rotation = 15
	}):Play()

end)

GearButton.MouseLeave:Connect(function()

	local Fade = Tween(Tooltip,0.15,{
		TextTransparency = 1
	})

	Fade:Play()

	Fade.Completed:Once(function()
		Tooltip.Visible = false
	end)

	Tween(GearButton,0.15,{
		Rotation = 0
	}):Play()

end)

--==================================================
-- Dragging
--==================================================

local Dragging = false
local DragInput
local DragStart
local StartPosition

local function UpdateDrag(Input)

	local Delta = Input.Position - DragStart

	Main.Position = UDim2.new(
		StartPosition.X.Scale,
		StartPosition.X.Offset + Delta.X,
		StartPosition.Y.Scale,
		StartPosition.Y.Offset + Delta.Y
	)

end

Main.InputBegan:Connect(function(Input)

	if Input.UserInputType == Enum.UserInputType.MouseButton1 then

		Dragging = true
		DragStart = Input.Position
		StartPosition = Main.Position

		Input.Changed:Connect(function()

			if Input.UserInputState == Enum.UserInputState.End then
				Dragging = false
			end

		end)

	end

end)

Main.InputChanged:Connect(function(Input)

	if Input.UserInputType == Enum.UserInputType.MouseMovement then
		DragInput = Input
	end

end)

UserInputService.InputChanged:Connect(function(Input)

	if Dragging and Input == DragInput then
		UpdateDrag(Input)
	end

end)
--==================================================
-- Settings Panel
--==================================================

local SettingsOpen = false

local SettingsPanel = Instance.new("Frame")
SettingsPanel.Name = "SettingsPanel"
SettingsPanel.Size = UDim2.fromOffset(180,165)
SettingsPanel.Position = UDim2.new(1,-10,0,0) -- Hidden
SettingsPanel.BackgroundColor3 = Theme.Background
SettingsPanel.BackgroundTransparency = Theme.BackgroundTransparency
SettingsPanel.BorderSizePixel = 0
SettingsPanel.ClipsDescendants = true
SettingsPanel.Parent = Main

local SettingsCorner = Instance.new("UICorner")
SettingsCorner.CornerRadius = UDim.new(0,12)
SettingsCorner.Parent = SettingsPanel

local SettingsStroke = Instance.new("UIStroke")
SettingsStroke.Color = Theme.Accent
SettingsStroke.Thickness = 1
SettingsStroke.Transparency = 0.35
SettingsStroke.Parent = SettingsPanel

local Padding = Instance.new("UIPadding")
Padding.PaddingTop = UDim.new(0,12)
Padding.PaddingLeft = UDim.new(0,12)
Padding.PaddingRight = UDim.new(0,12)
Padding.PaddingBottom = UDim.new(0,12)
Padding.Parent = SettingsPanel

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0,8)
Layout.FillDirection = Enum.FillDirection.Vertical
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Left
Layout.SortOrder = Enum.SortOrder.LayoutOrder
Layout.Parent = SettingsPanel

local SettingsTitle = Instance.new("TextLabel")
SettingsTitle.BackgroundTransparency = 1
SettingsTitle.Size = UDim2.new(1,0,0,24)
SettingsTitle.Font = Enum.Font.GothamBold
SettingsTitle.Text = "Settings"
SettingsTitle.TextSize = 17
SettingsTitle.TextColor3 = Theme.Text
SettingsTitle.TextXAlignment = Enum.TextXAlignment.Left
SettingsTitle.Parent = SettingsPanel

local Divider2 = Instance.new("Frame")
Divider2.Size = UDim2.new(1,0,0,1)
Divider2.BorderSizePixel = 0
Divider2.BackgroundColor3 = Theme.Accent
Divider2.BackgroundTransparency = 0.55
Divider2.Parent = SettingsPanel

--==================================================
-- Open / Close Animation
--==================================================

local OpenPosition = UDim2.new(1,8,0,0)
local ClosedPosition = UDim2.new(1,-10,0,0)

local function ToggleSettings()

	SettingsOpen = not SettingsOpen

	if SettingsOpen then

		Tween(
			SettingsPanel,
			0.25,
			{
				Position = OpenPosition
			}
		):Play()

		Tween(
			GearButton,
			0.25,
			{
				Rotation = 90
			}
		):Play()

	else

		Tween(
			SettingsPanel,
			0.25,
			{
				Position = ClosedPosition
			}
		):Play()

		Tween(
			GearButton,
			0.25,
			{
				Rotation = 0
			}
		):Play()

	end

end

GearButton.MouseButton1Click:Connect(ToggleSettings)
--==================================================
-- Toggle Creator
--==================================================

local Toggles = {}

local function CreateToggle(Text, Default, Callback)

	local Holder = Instance.new("Frame")
	Holder.Size = UDim2.new(1,0,0,28)
	Holder.BackgroundTransparency = 1
	Holder.Parent = SettingsPanel

	local Label = Instance.new("TextLabel")
	Label.BackgroundTransparency = 1
	Label.Size = UDim2.new(1,-58,1,0)
	Label.Font = Enum.Font.Gotham
	Label.Text = Text
	Label.TextSize = 14
	Label.TextColor3 = Theme.Text
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Holder

	local Switch = Instance.new("TextButton")
	Switch.AnchorPoint = Vector2.new(1,0.5)
	Switch.Position = UDim2.new(1,0,0.5,0)
	Switch.Size = UDim2.fromOffset(46,22)
	Switch.Text = ""
	Switch.AutoButtonColor = false
	Switch.BorderSizePixel = 0
	Switch.Parent = Holder

	local SwitchCorner = Instance.new("UICorner")
	SwitchCorner.CornerRadius = UDim.new(1,0)
	SwitchCorner.Parent = Switch

	local Knob = Instance.new("Frame")
	Knob.Size = UDim2.fromOffset(18,18)
	Knob.Position = UDim2.fromOffset(2,2)
	Knob.BackgroundColor3 = Color3.new(1,1,1)
	Knob.BorderSizePixel = 0
	Knob.Parent = Switch

	local KnobCorner = Instance.new("UICorner")
	KnobCorner.CornerRadius = UDim.new(1,0)
	KnobCorner.Parent = Knob

	local Enabled = Default

	local function Refresh(Animate)

		local BGColor = Enabled and Theme.Green or Theme.Red
		local KnobPos = Enabled
			and UDim2.fromOffset(26,2)
			or UDim2.fromOffset(2,2)

		if Animate then

			Tween(
				Switch,
				0.18,
				{
					BackgroundColor3 = BGColor
				}
			):Play()

			Tween(
				Knob,
				0.18,
				{
					Position = KnobPos,
					Size = UDim2.fromOffset(20,20)
				}
			):Play()

			task.delay(0.18,function()

				Tween(
					Knob,
					0.08,
					{
						Size = UDim2.fromOffset(18,18)
					}
				):Play()

			end)

		else

			Switch.BackgroundColor3 = BGColor
			Knob.Position = KnobPos

		end

	end

	Refresh(false)

	Switch.MouseButton1Click:Connect(function()

		Enabled = not Enabled

		Refresh(true)

		if Callback then
			Callback(Enabled)
		end

	end)

	Toggles[Text] = {
		Frame = Holder,
		Label = Label,
		Switch = Switch,
		Knob = Knob,

		Get = function()
			return Enabled
		end,

		Set = function(Value)

			Enabled = Value
			Refresh(true)

			if Callback then
				Callback(Value)
			end

		end
	}

	return Toggles[Text]

end
--==================================================
-- Settings
--==================================================

local DisplayPing = true
local DisplayFPS = true
local DisplayLB = game.PlaceId == 10449761463

CreateToggle("Display Ping", true, function(Value)
	DisplayPing = Value
end)

CreateToggle("Display FPS", true, function(Value)
	DisplayFPS = Value
end)

CreateToggle("Display LB Stats", DisplayLB, function(Value)
	DisplayLB = Value
end)
